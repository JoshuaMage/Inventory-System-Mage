<!-- <script>
    import Sales from '../../sales/+page.svelte';
    import MaterialStock from './+page.svelte';
    
    export let selectedItem; 
    export let materialPurchase;

    let selectedMaterialName = '';

    function handleSelectMaterial(event) {
        selectedMaterialName = event.detail; // Get the materialName from the event
    }
</script>

<MaterialStock on:selectMaterial={handleSelectMaterial} />
<Sales {selectedMaterialName} /> -->
